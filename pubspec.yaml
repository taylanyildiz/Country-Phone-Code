name: select_country_code_phone
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
    
  cupertino_icons: ^1.0.2
  country_picker: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true

